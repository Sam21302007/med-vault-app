{
  "name": "med-vault-root",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "postinstall": "cd my-app && npm install --legacy-peer-deps",
    "build": "cd my-app && npm run build",
    "start": "cd my-app && npm start"
  }
}
